package com.viettel.test.practice.dethat.cau11;

import java.util.function.Consumer;

public class Cau11 {
    public static void main(String[] args) {
        Cau11 clubMembers = new Cau11();
        /*long matches = clubMembers
                .peek(new Consumer<Member>() {
                    @Override
                    public void accept(Member m) {
                        m.print();
                    }
                })
                .filter(m -> m.getYearMemberShip() >= testMembershipLength)
                .map(m -> testName.compareToIgnoreCase(m))
                .filter(a -> a ==0)
                .count();
        System.out.println(matches);*/

    }
}
